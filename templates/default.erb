server {
  listen 80  default_server;
  server_name  _;

  access_log  /var/log/nginx/localhost.access.log;

  root   /var/www/simpleSinatra/public;
  passenger_enabled on;
  passenger_ruby /usr/bin/ruby;
}